/**
 * Ad Components - Easy import from a single location
 * 
 * Usage:
 * import { AdBanner, AdSection } from '../components/ads';
 */

export { AdBanner, AdSection } from './AdBanner';

// Re-export configuration for convenience
export { 
  ADSENSE_ENABLED, 
  ADSENSE_TEST_MODE,
  PAGE_ADS_ENABLED,
  AD_SLOTS,
} from '../../config/ads';


